The :meth:`Runner.build() <cocotb_tools.runner.Runner.build>` and :meth:`Runner.test() <cocotb_tools.runner.Runner.test>` methods now accept a ``log_file`` argument to redirect stdout and stderr to the specified file.
